# run for cmd
as the name suggests, this is the windows run prompt for the command line

## download
you can download the program [here](https://github.com/eightynine77/run-for-cmd/releases/latest)

## how to use
like windows run, you can use it in two ways:
1. use it with command arguments.
   for example:
   ```batchfile
   run notepad
   ```
   ```batchfile
   run "c:\path\to\your\file\your_file.txt"
   ```
   ```batchfile
   run "c:\folder\your-folder"
   ```
2. use its user input by running the program without arguments.
   for example:
   <img width="607" height="346" alt="image" src="https://github.com/user-attachments/assets/a51d230f-9137-4111-a2d3-d2854ab02a24" />
